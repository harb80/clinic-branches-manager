import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useLanguage } from "@/contexts/LanguageContext";
import { trpc } from "@/lib/trpc";
import { Building2, Plus, X } from "lucide-react";
import { useState } from "react";

export default function BranchesPage() {
  const { isArabic, direction } = useLanguage();
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState({ nameAr: "", nameEn: "", code: "", address: "", phone: "" });
  const branches = trpc.branches.list.useQuery();
  const create = trpc.branches.create.useMutation({ onSuccess: () => { setOpen(false); setForm({ nameAr: "", nameEn: "", code: "", address: "", phone: "" }); void branches.refetch(); } });
  const update = trpc.branches.update.useMutation({ onSuccess: () => void branches.refetch() });
  const set = (key: keyof typeof form, value: string) => setForm(current => ({ ...current, [key]: value }));
  return <DashboardLayout><div dir={direction} className="min-h-screen bg-[#f7fafc] p-4 text-slate-900 sm:p-8"><div className="mx-auto max-w-6xl space-y-6"><header className="flex flex-col gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-teal-700">{isArabic ? "ثلاثة فروع من منصة واحدة" : "Three branches, one platform"}</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">{isArabic ? "الفروع" : "Branches"}</h1><p className="mt-2 text-sm text-slate-500">{isArabic ? "إدارة بيانات الفروع والعناوين والحالة التشغيلية." : "Manage branch details, contact information, and operating status."}</p></div><Button onClick={() => setOpen(true)} className="gap-2 bg-slate-950 text-white hover:bg-slate-800"><Plus className="h-4 w-4" />{isArabic ? "إضافة فرع" : "Add branch"}</Button></header>{open && <Card className="border-teal-100 bg-white"><CardHeader className="flex flex-row items-center justify-between"><CardTitle>{isArabic ? "فرع جديد" : "New branch"}</CardTitle><Button variant="ghost" size="icon" onClick={() => setOpen(false)}><X className="h-4 w-4" /></Button></CardHeader><CardContent><form onSubmit={event => { event.preventDefault(); create.mutate(form); }} className="grid gap-4 md:grid-cols-2"><Input required placeholder="اسم الفرع بالعربية" value={form.nameAr} onChange={event => set("nameAr", event.target.value)} /><Input required placeholder="Branch name in English" value={form.nameEn} onChange={event => set("nameEn", event.target.value)} /><Input required placeholder={isArabic ? "كود الفرع" : "Branch code"} value={form.code} onChange={event => set("code", event.target.value)} /><Input placeholder={isArabic ? "رقم الهاتف" : "Phone"} value={form.phone} onChange={event => set("phone", event.target.value)} /><Input className="md:col-span-2" placeholder={isArabic ? "العنوان" : "Address"} value={form.address} onChange={event => set("address", event.target.value)} /><Button type="submit" disabled={create.isPending} className="bg-teal-700 text-white hover:bg-teal-800">{create.isPending ? (isArabic ? "جارٍ الحفظ..." : "Saving...") : (isArabic ? "حفظ الفرع" : "Save branch")}</Button></form>{create.error && <p className="mt-3 text-sm text-red-600">{isArabic ? "تعذر حفظ الفرع، تأكد من عدم تكرار الكود." : "Could not save branch. Check that the code is unique."}</p>}</CardContent></Card>}<Card className="border-0 bg-white shadow-[0_12px_35px_rgba(15,23,42,0.05)]"><CardHeader><CardTitle>{isArabic ? "الفروع المسجلة" : "Registered branches"}</CardTitle></CardHeader><CardContent>{branches.isLoading ? <p className="p-6 text-sm text-slate-400">{isArabic ? "جارٍ التحميل..." : "Loading..."}</p> : (branches.data ?? []).length === 0 ? <div className="flex min-h-[180px] flex-col items-center justify-center text-center text-slate-400"><Building2 className="mb-3 h-8 w-8" /><p>{isArabic ? "لم تتم إضافة فروع بعد" : "No branches added yet"}</p></div> : <div className="grid gap-4 md:grid-cols-3">{(branches.data ?? []).map(branch => <div key={branch.id} className="rounded-2xl border border-slate-100 bg-slate-50/70 p-5"><div className="flex items-start justify-between"><div><p className="font-semibold">{isArabic ? branch.nameAr : branch.nameEn}</p><p className="mt-1 text-xs text-slate-400">{branch.code}</p></div><span className={`rounded-full px-2 py-1 text-xs ${branch.isActive ? "bg-emerald-50 text-emerald-700" : "bg-slate-200 text-slate-500"}`}>{branch.isActive ? (isArabic ? "نشط" : "Active") : (isArabic ? "متوقف" : "Inactive")}</span></div><p className="mt-4 text-sm text-slate-500">{branch.address || (isArabic ? "لم يُحدد العنوان" : "Address not set")}</p><Button variant="outline" size="sm" className="mt-4" onClick={() => update.mutate({ id: branch.id, isActive: !branch.isActive })}>{branch.isActive ? (isArabic ? "إيقاف الفرع" : "Deactivate") : (isArabic ? "تفعيل الفرع" : "Activate")}</Button></div>)}</div>}</CardContent></Card></div></div></DashboardLayout>;
}
